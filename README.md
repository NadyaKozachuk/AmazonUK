Running Test:
Open the command prompt and navigate to the folder in which pom.xml file is present. Run the below Maven command.

mvn test
Once the execution completes report & screenshots will be generated in below folder.

Report: target/cucumber.html
Screenshot: test-result/report (can be set to defauld folder build/reports/tests)
